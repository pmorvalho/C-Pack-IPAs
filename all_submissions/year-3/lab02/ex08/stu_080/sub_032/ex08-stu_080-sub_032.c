
#include <stdio.h>
int main(){
    int num, i;
    float n1, soma = 0, media;
    scanf("%d", &num);
    for (i == 0; i < num; i++){
        scanf("%f", &n1);
        soma += n1;
    }
    media = soma / i;
    printf("%.2f\n", media);
    return 0; 
}