

#include <stdio.h>
#define DIM 100

int main() {
    int c, resultado;
    char palavra[DIM];
    scanf( "%s", palavra);
    while ((c = getchar()) != '\0') {
        if (c = '+')
        if (c = '-')
    }

}