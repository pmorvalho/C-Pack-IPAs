

#include <stdio.h>

int main(){
    int n, valor, menor, maior;
    scanf("%d\n", n);
    scanf("%d\n", valor);
    valor = menor = maior
    while(n>0){
        scanf("%d\n", valor);
        if(valor>maior)
            valor = maior
        if(valor<menor)
            valor = menor
        n--;
    }
    return 0;
}
