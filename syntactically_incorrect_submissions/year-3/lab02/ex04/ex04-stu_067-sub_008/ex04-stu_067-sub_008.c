
#include <stdio.h>
#define DIM 3

int main() 
{
    int tab[DIM]
    for (i=0; i<DIM; i++){
        scanf("%d", &tab[i]);
        }
        
    for (i=0; i<DIM; i++){
        if (tab[i]>tab[i+1])
             tab[i] = tab[i+1];
    }

    printf("%d",tab[0]>tab[2]? )
}