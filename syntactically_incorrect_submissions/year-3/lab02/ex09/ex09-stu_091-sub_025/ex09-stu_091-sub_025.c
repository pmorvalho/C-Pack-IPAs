
#include <stdio.h>

int main()
{
    float X , s,m,h, seg = 0.0 , min = 0.0 , hora = 0.0;
    scanf("%f", &X);
    s = X % 60;

    if (s != 0)
    {
        seg = X % 60;
    }
    seg = X % 60;
    min = (X - seg) /
}